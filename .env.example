# Production Environment Variables Template
# Copy this file to .env and fill in your production values

# ==========================================
# REQUIRED FOR PRODUCTION DEPLOYMENT
# ==========================================

# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database
PGHOST=your-postgres-host
PGPORT=5432
PGUSER=your-postgres-user
PGPASSWORD=your-postgres-password
PGDATABASE=your-database-name

# JWT Authentication (CRITICAL - Generate secure secrets)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-minimum-32-characters

# ==========================================
# OPTIONAL PRODUCTION SETTINGS
# ==========================================

# Environment
NODE_ENV=production

# CORS Configuration (comma-separated list of allowed domains)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Session Configuration
SESSION_SECRET=your-secure-session-secret

# ==========================================
# DEPLOYMENT INSTRUCTIONS
# ==========================================

# 1. Generate secure JWT secret:
#    openssl rand -base64 32

# 2. Set up PostgreSQL database with SSL enabled

# 3. Configure your hosting platform with these environment variables

# 4. Enable HTTPS/SSL certificates

# 5. Set up monitoring and logging

# ==========================================
# SECURITY NOTES
# ==========================================

# - Never commit real secrets to version control
# - Use secure, randomly generated secrets
# - Enable database SSL connections in production
# - Regular security audits recommended
# - Monitor authentication attempts and failures